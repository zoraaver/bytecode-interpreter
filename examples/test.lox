// this is a comment

fun outer(x, y, z) {
    fun sum() {
        return x + y + z;
    }

    return sum;
}

for (var i = 0; i < 1000; i = i + 1) {
    var x = outer(i, i * 2, i * 3);
    var z = "this should " + "be collected eventually";

    print(z);
    print(x());
}
