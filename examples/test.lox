// this is a comment
// !(5 - 4 > 3 * 2 == !nil)
fun outer(x, y) {
    fun allocate() {
        return x + y;
    }

    return allocate;
}

for (var i = 0; i < 1000; i = i + 1) {
    var x = outer(i, i + 1);

    print(x());
}
